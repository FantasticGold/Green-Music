<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_default">

    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/iv_left1_music_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:scaleType="fitCenter"
        android:src="@drawable/bg_left2_music"/>

    <ImageView
        android:id="@+id/iv_left1_music_back"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignEnd="@id/iv_left1_music_bg"
        android:layout_alignRight="@id/iv_left1_music_bg"
        android:layout_alignBottom="@id/iv_left1_music_bg"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/btn_music_back" />

</RelativeLayout>